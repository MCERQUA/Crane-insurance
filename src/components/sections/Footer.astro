---
const currentYear = new Date().getFullYear();

const footerLinks = {
  company: [
    { label: 'About', href: '#' },
    { label: 'Coverage', href: '#services' },
    { label: 'Claims', href: '/blog' },
  ],
  support: [
    { label: 'Contact', href: '#contact-form' },
    { label: 'FAQ', href: '#' },
    { label: 'Privacy', href: '#' },
  ],
  social: [
    { label: 'Facebook', href: '#' },
    { label: 'Instagram', href: '#' },
    { label: 'LinkedIn', href: '#' },
  ]
};
---

<footer class="bg-charcoal-black text-aluminum py-16">
  <div class="container-padding max-w-7xl mx-auto">
    <div class="grid grid-cols-1 md:grid-cols-4 gap-8 mb-12">
      <!-- Brand -->
      <div class="space-y-4">
        <a href="/" class="inline-block">
          <img 
            src="/images/logos/SPRAY-COMFORT-INSULAITON-ARIZONA-LG-LOGO2.png" 
            alt="Comfort Insulation" 
            class="h-16 w-auto brightness-110"
          />
        </a>
        <p class="text-concrete-gray/70 text-sm">
          Comprehensive crane insurance for heavy equipment operations.
        </p>
      </div>
      
      <!-- Company Links -->
      <div>
        <h3 class="font-semibold mb-4 text-construction-yellow">Company</h3>
        <ul class="space-y-2">
          {footerLinks.company.map((link) => (
            <li>
              <a 
                href={link.href}
                class="text-concrete-gray/60 hover:text-construction-yellow transition-colors duration-300 text-sm"
              >
                {link.label}
              </a>
            </li>
          ))}
        </ul>
      </div>
      
      <!-- Support Links -->
      <div>
        <h3 class="font-semibold mb-4 text-construction-yellow">Support</h3>
        <ul class="space-y-2">
          {footerLinks.support.map((link) => (
            <li>
              <a 
                href={link.href}
                class="text-concrete-gray/60 hover:text-construction-yellow transition-colors duration-300 text-sm"
              >
                {link.label}
              </a>
            </li>
          ))}
        </ul>
      </div>
      
      <!-- Social Links -->
      <div>
        <h3 class="font-semibold mb-4 text-construction-yellow">Connect</h3>
        <ul class="space-y-2">
          {footerLinks.social.map((link) => (
            <li>
              <a 
                href={link.href}
                class="text-concrete-gray/60 hover:text-construction-yellow transition-colors duration-300 text-sm"
              >
                {link.label}
              </a>
            </li>
          ))}
        </ul>
      </div>
    </div>
    
    <!-- Divider -->
    <div class="border-t border-machinery-gray/20 pt-8">
      <div class="flex flex-col md:flex-row justify-between items-center gap-4">
        <p class="text-concrete-gray/50 text-sm">
          © {currentYear} Heavy Crane Insurance. All rights reserved.
        </p>
        <p class="text-concrete-gray/50 text-sm">
          Licensed • Bonded • DOI Compliant
        </p>
      </div>
    </div>
  </div>
</footer>
